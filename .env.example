# Personal access token for the Supabase MCP server (see .mcp.json).
# Create one at https://supabase.com/dashboard/account/tokens, scoped to the
# sterlex-dev project only. Never use a token that has access to production.
SUPABASE_ACCESS_TOKEN=your-supabase-personal-access-token
